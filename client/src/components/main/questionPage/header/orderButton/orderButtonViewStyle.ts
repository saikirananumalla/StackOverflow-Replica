import styled from "styled-components";
import { Button } from "@mui/material";

export const QuestionOrderButton = styled(Button)`
  && {
    margin-left: 2px;
    padding: 4px 4px;
    font-size: x-small;
  }
`;